<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ScacirBeta" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/primario</item>
    </style>

    <style name="Theme.ScacirBeta" parent="Base.Theme.ScacirBeta" />

    <style name="Theme.ScacirBeta.Fullscreen" parent="Theme.ScacirBeta">
        <item name="android:actionBarStyle">@style/Widget.Theme.ScacirBeta.ActionBar.Fullscreen
        </item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

</resources>